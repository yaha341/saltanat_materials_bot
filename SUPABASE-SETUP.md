# 🚀 Полная настройка Supabase для Telegram бота

## 📋 Обзор

Этот документ содержит полную инструкцию по настройке Supabase для вашего Telegram бота с админ-панелью.

## 🔧 Шаг 1: Выполнение полной настройки базы данных

### 1.1 Откройте SQL Editor
Перейдите по ссылке: https://supabase.com/dashboard/project/nytjbxtbmtadmqentgca/sql

### 1.2 Создайте новый запрос
Нажмите кнопку **"New Query"**

### 1.3 Выполните полный скрипт настройки
Откройте файл `COMPLETE-SETUP.sql` и скопируйте весь код в SQL Editor, затем нажмите **"Run"**.

**Или выполните по частям:**

#### Часть 1: Таблицы базы данных
Скопируйте и выполните код из раздела "ЧАСТЬ 1: Создание таблиц базы данных"

#### Часть 2: Storage Buckets
Скопируйте и выполните код из раздела "ЧАСТЬ 2: Создание Storage Buckets"

#### Часть 3: RLS Policies для Storage
Скопируйте и выполните код из раздела "ЧАСТЬ 3: RLS Policies для Storage"

#### Часть 4: RLS Policies для таблиц
Скопируйте и выполните код из раздела "ЧАСТЬ 4: RLS Policies для таблиц"

## ✅ Что будет создано:

### 📊 Таблицы базы данных:
- `categories` - категории с вложенностью
- `products` - товары с описанием, ценой, файлами
- `product_images` - изображения товаров
- `payment_methods` - способы оплаты по странам
- `bot_users` - пользователи Telegram бота
- `cart_items` - корзина пользователей
- `orders` - заказы
- `order_items` - позиции в заказах
- `app_settings` - настройки приложения

### 📁 Storage Buckets:
- `product-images` - для изображений товаров (публичный, 5MB)
- `product-files` - для файлов товаров (публичный, 50MB)
- `payment-proofs` - для скриншотов оплаты (приватный, 5MB)

### 🔒 Security:
- RLS policies для всех таблиц
- RLS policies для storage buckets
- Индексы для быстрого поиска
- Триггеры для автоматического обновления timestamp

### 🌍 Начальные данные:
- Способы оплаты для Казахстана, России, Кыргызстана, Беларуси и других стран

## 🧪 Проверка настройки

### Проверка таблиц:
После выполнения скрипта выполните этот запрос для проверки:

```sql
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
ORDER BY table_name;
```

Должны увидеть все 9 таблиц.

### Проверка storage buckets:
```sql
SELECT id, name, public 
FROM storage.buckets 
ORDER BY id;
```

Должны увидеть 3 buckets.

### Проверка RLS policies:
```sql
SELECT schemaname, tablename, policyname 
FROM pg_policies 
WHERE schemaname = 'public' 
ORDER BY tablename, policyname;
```

Должны увидеть политики для всех таблиц.

## 🚀 После успешной настройки:

1. **Перезапустите сервер разработки** (если он запущен)
2. **Откройте админ-панель**: http://localhost:8080/admin
3. **Войдите** с логином `admin` и паролем `admin`
4. **Попробуйте добавить категорию** - теперь должно работать!

## 📝 Настройка способов оплаты:

После настройки базы данных перейдите в админ-панель:
1. Откройте раздел **"Реквизиты"**
2. Отредактируйте способы оплаты для каждой страны
3. Укажите ваши реальные реквизиты вместо примеров

## 🎯 Следующие шаги:

1. ✅ Выполнить SQL скрипт настройки
2. ✅ Проверить создание таблиц и buckets
3. ✅ Протестировать добавление категории
4. ✅ Добавить способы оплаты с вашими реквизитами
5. ✅ Создать первую категорию товаров
6. ✅ Добавить первый товар с файлом

## ⚠️ Важные замечания:

- **Безопасность**: `.env` файл добавлен в `.gitignore` - ваши секреты не попадут в git
- **RLS Policies**: Все таблицы защищены RLS - доступ только через service_role
- **Storage**: Публичные buckets позволяют пользователям скачивать файлы без авторизации
- **Размеры файлов**: Ограничения установлены для защиты от злоупотреблений

## 🆘 Если возникнут проблемы:

1. **Ошибка "table does not exist"** - проверьте, что выполнили Часть 1 скрипта
2. **Ошибка "bucket does not exist"** - проверьте, что выполнили Часть 2 скрипта
3. **Ошибка "permission denied"** - проверьте, что выполнили Часть 3 и Часть 4 (RLS policies)
4. **Ошибка подключения** - проверьте `.env` файл с правильными учетными данными Supabase

## 📞 Поддержка:

Если что-то не работает - проверьте логи сервера и консоль браузера для ошибок.